export default function Info() {
  const styles = {
    color: "blue",
    fontWeight: "bold",
    fontSize: "30px",
    marginLeft: "30px",
    marginTop: "20px",
    marginBottom: "20px",
  };
  return (
    <div style={styles}>
      <div>Display Song Info from the JSON Server</div>
    </div>
  );
}
